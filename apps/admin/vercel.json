{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile=false && pnpm --filter admin build",
  "outputDirectory": "dist/browser",
  "framework": "angular",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
