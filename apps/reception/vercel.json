{
  "buildCommand": "cd ../.. && pnpm install --frozen-lockfile=false && pnpm --filter reception build",
  "outputDirectory": "dist/browser",
  "framework": "angular",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
